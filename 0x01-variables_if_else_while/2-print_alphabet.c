#include <stdio.h>
/**
 * main - this program will print the alphabet in
 * lowercase.
 *
 * return - otherwise will return null
 */
int main()
{
	char c;
	if (c == 'a' && c <= 'z')
	{
		putchar(c);
	}
	        return (0);
}
